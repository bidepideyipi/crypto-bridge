# 数据库表设计

## 版本说明

**当前版本：** v0.1.0 开发版

**存储方案：**
- **PostgreSQL**：主数据存储（地址映射、充值提现记录、余额账本）

---

## 一、PostgreSQL 表设计

### 1.1 用户地址表（user_addresses）

存储用户与钱包地址的映射关系。

```sql
CREATE TABLE user_addresses (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    chain VARCHAR(16) NOT NULL,
    address VARCHAR(128) NOT NULL,
    address_type VARCHAR(16) NOT NULL DEFAULT 'deposit', -- deposit, hot_wallet, cold_wallet
    status VARCHAR(16) NOT NULL DEFAULT 'active', -- active, inactive
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    UNIQUE (user_id, chain, address_type)
);

CREATE INDEX idx_user_addresses_user_id ON user_addresses(user_id);
CREATE INDEX idx_user_addresses_address ON user_addresses(address);
CREATE INDEX idx_user_addresses_chain ON user_addresses(chain);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| user_id | VARCHAR(64) | 用户 ID |
| chain | VARCHAR(16) | 链类型：BTC, ETH |
| address | VARCHAR(128) | 钱包地址 |
| address_type | VARCHAR(16) | 地址类型：deposit(充值), hot_wallet(热钱包), cold_wallet(冷钱包) |
| status | VARCHAR(16) | 状态：active(活跃), inactive(停用) |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |

---

### 1.2 充值记录表（deposits）

存储链上充值交易记录。

```sql
CREATE TABLE deposits (
    id BIGSERIAL PRIMARY KEY,
    deposit_id VARCHAR(64) NOT NULL UNIQUE,
    user_id VARCHAR(64) NOT NULL,
    chain VARCHAR(16) NOT NULL,
    tx_hash VARCHAR(128) NOT NULL,
    from_address VARCHAR(128),
    to_address VARCHAR(128) NOT NULL,
    amount NUMERIC(36, 18) NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'pending', -- pending, confirmed, completed
    confirmations INTEGER NOT NULL DEFAULT 0,
    required_confirmations INTEGER NOT NULL,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    completed_at BIGINT
);

CREATE INDEX idx_deposits_user_id ON deposits(user_id);
CREATE INDEX idx_deposits_tx_hash ON deposits(tx_hash);
CREATE INDEX idx_deposits_status ON deposits(status);
CREATE INDEX idx_deposits_chain ON deposits(chain);
CREATE INDEX idx_deposits_created_at ON deposits(created_at);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| deposit_id | VARCHAR(64) | 充值 ID（业务唯一标识） |
| user_id | VARCHAR(64) | 用户 ID |
| chain | VARCHAR(16) | 链类型：BTC, ETH |
| tx_hash | VARCHAR(128) | 链上交易哈希 |
| from_address | VARCHAR(128) | 来源地址 |
| to_address | VARCHAR(128) | 目标地址（充值地址） |
| amount | NUMERIC(36,18) | 充值金额（高精度） |
| status | VARCHAR(16) | 状态：pending(待确认), confirmed(已确认), completed(已完成) |
| confirmations | INTEGER | 当前确认数 |
| required_confirmations | INTEGER | 需要的确认数 |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |
| completed_at | BIGINT | 完成时间（Unix 时间戳） |

---

### 1.3 提现记录表（withdrawals）

存储用户提现申请记录。

```sql
CREATE TABLE withdrawals (
    id BIGSERIAL PRIMARY KEY,
    withdraw_id VARCHAR(64) NOT NULL UNIQUE,
    user_id VARCHAR(64) NOT NULL,
    chain VARCHAR(16) NOT NULL,
    to_address VARCHAR(128) NOT NULL,
    amount NUMERIC(36, 18) NOT NULL,
    fee NUMERIC(36, 18) NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'pending', -- pending, approved, rejected, completed, failed
    tx_hash VARCHAR(128),
    confirmations INTEGER NOT NULL DEFAULT 0,
    required_confirmations INTEGER NOT NULL,
    memo TEXT,
    reject_reason TEXT,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    approved_at BIGINT,
    completed_at BIGINT
);

CREATE INDEX idx_withdrawals_user_id ON withdrawals(user_id);
CREATE INDEX idx_withdrawals_status ON withdrawals(status);
CREATE INDEX idx_withdrawals_chain ON withdrawals(chain);
CREATE INDEX idx_withdrawals_tx_hash ON withdrawals(tx_hash);
CREATE INDEX idx_withdrawals_created_at ON withdrawals(created_at);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| withdraw_id | VARCHAR(64) | 提现 ID（业务唯一标识） |
| user_id | VARCHAR(64) | 用户 ID |
| chain | VARCHAR(16) | 链类型：BTC, ETH |
| to_address | VARCHAR(128) | 提现目标地址 |
| amount | NUMERIC(36,18) | 提现金额（高精度） |
| fee | NUMERIC(36,18) | 手续费（高精度） |
| status | VARCHAR(16) | 状态：pending(待审批), approved(已批准), rejected(已拒绝), completed(已完成), failed(失败) |
| tx_hash | VARCHAR(128) | 链上交易哈希 |
| confirmations | INTEGER | 当前确认数 |
| required_confirmations | INTEGER | 需要的确认数 |
| memo | TEXT | 用户备注 |
| reject_reason | TEXT | 拒绝原因 |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |
| approved_at | BIGINT | 批准时间（Unix 时间戳） |
| completed_at | BIGINT | 完成时间（Unix 时间戳） |

---

### 1.4 用户余额表（user_balances）

存储用户在系统中的余额账本。

```sql
CREATE TABLE user_balances (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    chain VARCHAR(16) NOT NULL,
    balance NUMERIC(36, 18) NOT NULL DEFAULT 0,
    locked_balance NUMERIC(36, 18) NOT NULL DEFAULT 0,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    UNIQUE (user_id, chain)
);

CREATE INDEX idx_user_balances_user_id ON user_balances(user_id);
CREATE INDEX idx_user_balances_chain ON user_balances(chain);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| user_id | VARCHAR(64) | 用户 ID |
| chain | VARCHAR(16) | 链类型：BTC, ETH |
| balance | NUMERIC(36,18) | 可用余额 |
| locked_balance | NUMERIC(36,18) | 冻结余额（提现中） |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |

---

### 1.5 余额变更流水表（balance_transactions）

记录所有余额变更的流水，用于对账和审计。

```sql
CREATE TABLE balance_transactions (
    id BIGSERIAL PRIMARY KEY,
    transaction_id VARCHAR(64) NOT NULL UNIQUE,
    user_id VARCHAR(64) NOT NULL,
    chain VARCHAR(16) NOT NULL,
    type VARCHAR(16) NOT NULL, -- deposit, withdraw, freeze, unfreeze, transfer
    amount NUMERIC(36, 18) NOT NULL,
    balance_before NUMERIC(36, 18) NOT NULL,
    balance_after NUMERIC(36, 18) NOT NULL,
    related_id VARCHAR(64), -- 关联的 deposit_id 或 withdraw_id
    created_at BIGINT NOT NULL
);

CREATE INDEX idx_balance_transactions_user_id ON balance_transactions(user_id);
CREATE INDEX idx_balance_transactions_chain ON balance_transactions(chain);
CREATE INDEX idx_balance_transactions_type ON balance_transactions(type);
CREATE INDEX idx_balance_transactions_related_id ON balance_transactions(related_id);
CREATE INDEX idx_balance_transactions_created_at ON balance_transactions(created_at);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| transaction_id | VARCHAR(64) | 流水 ID（业务唯一标识） |
| user_id | VARCHAR(64) | 用户 ID |
| chain | VARCHAR(16) | 链类型 |
| type | VARCHAR(16) | 变更类型：deposit(充值), withdraw(提现), freeze(冻结), unfreeze(解冻), transfer(转账) |
| amount | NUMERIC(36,18) | 变更金额（正数为增加，负数为减少） |
| balance_before | NUMERIC(36,18) | 变更前余额 |
| balance_after | NUMERIC(36,18) | 变更后余额 |
| related_id | VARCHAR(64) | 关联的充值或提现 ID |
| created_at | BIGINT | 创建时间（Unix 时间戳） |

---

### 1.6 链上交易追踪表（chain_transactions）

追踪链上交易的处理状态。

```sql
CREATE TABLE chain_transactions (
    id BIGSERIAL PRIMARY KEY,
    chain VARCHAR(16) NOT NULL,
    tx_hash VARCHAR(128) NOT NULL,
    type VARCHAR(16) NOT NULL, -- deposit, withdraw, archive
    status VARCHAR(16) NOT NULL DEFAULT 'pending', -- pending, confirmed, failed
    confirmations INTEGER NOT NULL DEFAULT 0,
    required_confirmations INTEGER NOT NULL,
    raw_tx JSONB, -- 原始交易数据
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    UNIQUE (chain, tx_hash)
);

CREATE INDEX idx_chain_transactions_chain ON chain_transactions(chain);
CREATE INDEX idx_chain_transactions_tx_hash ON chain_transactions(tx_hash);
CREATE INDEX idx_chain_transactions_status ON chain_transactions(status);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| chain | VARCHAR(16) | 链类型 |
| tx_hash | VARCHAR(128) | 交易哈希 |
| type | VARCHAR(16) | 交易类型：deposit(充值), withdraw(提现), archive(归档) |
| status | VARCHAR(16) | 状态：pending(待确认), confirmed(已确认), failed(失败) |
| confirmations | INTEGER | 当前确认数 |
| required_confirmations | INTEGER | 需要的确认数 |
| raw_tx | JSONB | 原始交易数据 |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |

---

### 1.7 系统配置表（system_configs）

存储系统配置参数。

```sql
CREATE TABLE system_configs (
    id BIGSERIAL PRIMARY KEY,
    config_key VARCHAR(64) NOT NULL UNIQUE,
    config_value TEXT NOT NULL,
    description TEXT,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL
);

CREATE INDEX idx_system_configs_key ON system_configs(config_key);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| config_key | VARCHAR(64) | 配置键 |
| config_value | TEXT | 配置值（JSON 格式） |
| description | TEXT | 配置描述 |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |

**预置配置示例：**

| config_key | config_value |
|------------|--------------|
| btc.min_deposit | "1000" |
| btc.max_withdraw | "10000000" |
| btc.confirmations | "6" |
| eth.min_deposit | "1000000000000000" |
| eth.max_withdraw | "10000000000000000000" |
| eth.confirmations | "12" |

---

### 1.8 钱包配置表（wallet_configs）

存储冷热钱包的配置信息。

```sql
CREATE TABLE wallet_configs (
    id BIGSERIAL PRIMARY KEY,
    chain VARCHAR(16) NOT NULL,
    wallet_type VARCHAR(16) NOT NULL, -- hot_wallet, cold_wallet
    address VARCHAR(128) NOT NULL,
    config JSONB NOT NULL, -- 钱包配置（多签信息、HSM配置等）
    status VARCHAR(16) NOT NULL DEFAULT 'active', -- active, inactive, maintenance
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    UNIQUE (chain, wallet_type, address)
);

CREATE INDEX idx_wallet_configs_chain ON wallet_configs(chain);
CREATE INDEX idx_wallet_configs_type ON wallet_configs(wallet_type);
CREATE INDEX idx_wallet_configs_status ON wallet_configs(status);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| chain | VARCHAR(16) | 链类型：BTC, ETH |
| wallet_type | VARCHAR(16) | 钱包类型：hot_wallet(热钱包), cold_wallet(冷钱包) |
| address | VARCHAR(128) | 钱包地址 |
| config | JSONB | 钱包配置：{threshold, signers, hsm_key_id, shamir_shares} |
| status | VARCHAR(16) | 状态：active(活跃), inactive(停用), maintenance(维护中) |
| created_at | BIGINT | 创建时间（Unix 时间戳） |
| updated_at | BIGINT | 更新时间（Unix 时间戳） |

**config 字段结构示例：**

```json
// HSM 热钱包配置
{
  "sign_type": "hsm",
  "hsm_key_id": "aws-kms-key-123",
  "auto_sign": true
}

// 多签冷钱包配置
{
  "sign_type": "multisig",
  "m": 3,
  "n": 5,
  "signers": [
    {"id": "signer_1", "pubkey": "0x...", "name": "财务-张三"},
    {"id": "signer_2", "pubkey": "0x...", "name": "财务-李四"},
    {"id": "signer_3", "pubkey": "0x...", "name": "CEO"},
    {"id": "signer_4", "pubkey": "0x...", "name": "CTO"},
    {"id": "signer_5", "pubkey": "0x...", "name": "安全总监"}
  ]
}

// Shamir 秘钥分片配置
{
  "sign_type": "shamir",
  "threshold": 3,
  "shares": 5,
  "share_holders": [
    {"id": "holder_1", "name": "财务-张三"},
    {"id": "holder_2", "name": "财务-李四"},
    {"id": "holder_3", "name": "CEO"},
    {"id": "holder_4", "name": "CTO"},
    {"id": "holder_5", "name": "安全总监"}
  ]
}
```

---

### 1.9 归档记录表（archive_records）

记录热钱包到冷钱包的归档操作记录。

```sql
CREATE TABLE archive_records (
    id BIGSERIAL PRIMARY KEY,
    archive_id VARCHAR(64) NOT NULL UNIQUE,
    chain VARCHAR(16) NOT NULL,
    from_address VARCHAR(128) NOT NULL, -- 热钱包地址
    to_address VARCHAR(128) NOT NULL, -- 冷钱包地址
    amount NUMERIC(36, 18) NOT NULL,
    tx_hash VARCHAR(128),
    status VARCHAR(16) NOT NULL DEFAULT 'pending', -- pending, confirmed, failed
    confirmations INTEGER NOT NULL DEFAULT 0,
    required_confirmations INTEGER NOT NULL,
    trigger_reason VARCHAR(32) NOT NULL, -- threshold, scheduled, manual
    trigger_balance NUMERIC(36, 18), -- 触发时的热钱包余额
    threshold NUMERIC(36, 18), -- 配置的归档阈值
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    confirmed_at BIGINT
);

CREATE INDEX idx_archive_records_chain ON archive_records(chain);
CREATE INDEX idx_archive_records_status ON archive_records(status);
CREATE INDEX idx_archive_records_tx_hash ON archive_records(tx_hash);
CREATE INDEX idx_archive_records_created_at ON archive_records(created_at);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| archive_id | VARCHAR(64) | 归档 ID（业务唯一标识） |
| chain | VARCHAR(16) | 链类型 |
| from_address | VARCHAR(128) | 热钱包地址 |
| to_address | VARCHAR(128) | 冷钱包地址 |
| amount | NUMERIC(36,18) | 归档金额 |
| tx_hash | VARCHAR(128) | 链上交易哈希 |
| status | VARCHAR(16) | 状态：pending(待确认), confirmed(已确认), failed(失败) |
| confirmations | INTEGER | 当前确认数 |
| required_confirmations | INTEGER | 需要的确认数 |
| trigger_reason | VARCHAR(32) | 触发原因：threshold(超阈值), scheduled(定时), manual(手动) |
| trigger_balance | NUMERIC(36,18) | 触发时的热钱包余额 |
| threshold | NUMERIC(36,18) | 配置的归档阈值 |
| created_at | BIGINT | 创建时间 |
| updated_at | BIGINT | 更新时间 |
| confirmed_at | BIGINT | 确认时间 |

---

### 1.10 多签审批表（multisig_approvals）

记录多签审批的操作记录。

```sql
CREATE TABLE multisig_approvals (
    id BIGSERIAL PRIMARY KEY,
    approval_id VARCHAR(64) NOT NULL UNIQUE,
    related_type VARCHAR(16) NOT NULL, -- withdrawal, archive
    related_id VARCHAR(64) NOT NULL, -- 关联的 withdraw_id 或 archive_id
    chain VARCHAR(16) NOT NULL,
    required_signatures INTEGER NOT NULL, -- 需要的签名数 m
    total_signers INTEGER NOT NULL, -- 总签名者数 n
    tx_data TEXT NOT NULL, -- 未签名的交易数据（十六进制）
    status VARCHAR(16) NOT NULL DEFAULT 'pending', -- pending, approved, rejected, expired
    expire_at BIGINT NOT NULL, -- 审批过期时间
    created_at BIGINT NOT NULL,
    updated_at BIGINT,
    completed_at BIGINT
);

CREATE INDEX idx_multisig_approvals_related ON multisig_approvals(related_type, related_id);
CREATE INDEX idx_multisig_approvals_status ON multisig_approvals(status);
CREATE INDEX idx_multisig_approvals_chain ON multisig_approvals(chain);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| approval_id | VARCHAR(64) | 审批 ID |
| related_type | VARCHAR(16) | 关联类型：withdrawal(提现), archive(归档) |
| related_id | VARCHAR(64) | 关联的业务 ID |
| chain | VARCHAR(16) | 链类型 |
| required_signatures | INTEGER | 需要的签名数（m of n） |
| total_signers | INTEGER | 总签名者数（n） |
| tx_data | TEXT | 未签名的交易数据 |
| status | VARCHAR(16) | 状态：pending(待签名), approved(已通过), rejected(已拒绝), expired(已过期) |
| expire_at | BIGINT | 审批过期时间 |
| created_at | BIGINT | 创建时间 |
| updated_at | BIGINT | 更新时间 |
| completed_at | BIGINT | 完成时间 |

---

### 1.11 多签签名记录表（multisig_signatures）

记录每个签名者的签名记录。

```sql
CREATE TABLE multisig_signatures (
    id BIGSERIAL PRIMARY KEY,
    approval_id VARCHAR(64) NOT NULL,
    signer_id VARCHAR(64) NOT NULL,
    signature VARCHAR(256) NOT NULL, -- 签名数据
    signed_at BIGINT NOT NULL,
    created_at BIGINT NOT NULL,
    UNIQUE (approval_id, signer_id)
);

CREATE INDEX idx_multisig_signatures_approval ON multisig_signatures(approval_id);
CREATE INDEX idx_multisig_signatures_signer ON multisig_signatures(signer_id);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| approval_id | VARCHAR(64) | 审批 ID |
| signer_id | VARCHAR(64) | 签名者 ID |
| signature | VARCHAR(256) | 签名数据 |
| signed_at | BIGINT | 签名时间 |
| created_at | BIGINT | 创建时间 |

---

### 1.12 归档策略配置表（archive_policies）

配置各链的自动归档策略。

```sql
CREATE TABLE archive_policies (
    id BIGSERIAL PRIMARY KEY,
    chain VARCHAR(16) NOT NULL UNIQUE,
    enabled BOOLEAN NOT NULL DEFAULT true,
    threshold NUMERIC(36, 18) NOT NULL, -- 触发归档的阈值
    reserve_amount NUMERIC(36, 18) NOT NULL DEFAULT 0, -- 保留金额（不归档）
    min_archive_amount NUMERIC(36, 18) NOT NULL, -- 最小归档金额
    schedule VARCHAR(64), -- 定时归档策略（cron 表达式）
    required_confirmations INTEGER NOT NULL DEFAULT 6,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL
);

CREATE INDEX idx_archive_policies_chain ON archive_policies(chain);
CREATE INDEX idx_archive_policies_enabled ON archive_policies(enabled);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| chain | VARCHAR(16) | 链类型 |
| enabled | BOOLEAN | 是否启用自动归档 |
| threshold | NUMERIC(36,18) | 触发归档的阈值 |
| reserve_amount | NUMERIC(36,18) | 保留金额（热钱包保留） |
| min_archive_amount | NUMERIC(36,18) | 最小归档金额 |
| schedule | VARCHAR(64) | 定时归档策略 |
| required_confirmations | INTEGER | 需要的确认数 |
| created_at | BIGINT | 创建时间 |
| updated_at | BIGINT | 更新时间 |

---

### 1.13 审批者信息表（approvers）

存储多签审批者的信息。

```sql
CREATE TABLE approvers (
    id BIGSERIAL PRIMARY KEY,
    approver_id VARCHAR(64) NOT NULL UNIQUE,
    name VARCHAR(64) NOT NULL,
    email VARCHAR(128),
    pubkey TEXT, -- 公钥（用于多签验证）
    status VARCHAR(16) NOT NULL DEFAULT 'active', -- active, inactive
    role VARCHAR(32), -- finance, security, executive
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL
);

CREATE INDEX idx_approvers_status ON approvers(status);
CREATE INDEX idx_approvers_role ON approvers(role);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| approver_id | VARCHAR(64) | 审批者 ID |
| name | VARCHAR(64) | 姓名 |
| email | VARCHAR(128) | 邮箱（用于通知） |
| pubkey | TEXT | 公钥 |
| status | VARCHAR(16) | 状态：active(活跃), inactive(停用) |
| role | VARCHAR(32) | 角色：finance(财务), security(安全), executive(高管) |
| created_at | BIGINT | 创建时间 |
| updated_at | BIGINT | 更新时间 |

---

### 1.14 审批日志表（approval_logs）

记录所有审批操作的日志，用于审计。

```sql
CREATE TABLE approval_logs (
    id BIGSERIAL PRIMARY KEY,
    approval_id VARCHAR(64) NOT NULL,
    approver_id VARCHAR(64),
    action VARCHAR(32) NOT NULL, -- created, signed, approved, rejected, expired
    details JSONB,
    ip_address INET,
    user_agent TEXT,
    created_at BIGINT NOT NULL
);

CREATE INDEX idx_approval_logs_approval ON approval_logs(approval_id);
CREATE INDEX idx_approval_logs_created_at ON approval_logs(created_at);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| approval_id | VARCHAR(64) | 审批 ID |
| approver_id | VARCHAR(64) | 审批者 ID |
| action | VARCHAR(32) | 操作类型 |
| details | JSONB | 详细信息 |
| ip_address | INET | 操作 IP 地址 |
| user_agent | TEXT | 用户代理 |
| created_at | BIGINT | 创建时间 |

---

### 1.15 冷钱包余额追踪表（cold_wallet_balances）

追踪冷钱包的链上余额。

```sql
CREATE TABLE cold_wallet_balances (
    id BIGSERIAL PRIMARY KEY,
    chain VARCHAR(16) NOT NULL,
    address VARCHAR(128) NOT NULL,
    balance NUMERIC(36, 18) NOT NULL DEFAULT 0,
    updated_at BIGINT NOT NULL,
    UNIQUE (chain, address)
);

CREATE INDEX idx_cold_wallet_balances_chain ON cold_wallet_balances(chain);
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| chain | VARCHAR(16) | 链类型 |
| address | VARCHAR(128) | 冷钱包地址 |
| balance | NUMERIC(36,18) | 链上余额 |
| updated_at | BIGINT | 更新时间 |

---

## 三、数据访问策略

### 3.1 读写分离

```
写操作 → 主库
读操作 → 从库（延迟 < 1s）
```

**适用读操作：**
- 查询用户地址
- 查询充值/提现记录
- 查询余额流水

**必须走主库：**
- 用户余额扣减（需要最新值）
- 提现状态更新


### 3.3 事务边界

**充值入账事务：**
```
BEGIN
1. 更新 deposits 状态
2. 插入 balance_transactions
3. 更新 user_balances
COMMIT
```

**提现创建事务：**
```
BEGIN
1. 插入 withdrawals
2. 更新 user_balances（冻结余额）
3. 插入 balance_transactions
COMMIT
```

---

## 四、数据归档策略

### 4.1 归档规则

| 数据类型 | 归档周期 | 保留策略 |
|---------|---------|---------|
| 充值记录 | 3 个月后 | 保留 3 年 |
| 提现记录 | 3 个月后 | 保留 3 年 |
| 余额流水 | 6 个月后 | 保留 5 年 |
| 链上交易追踪 | 1 个月后 | 保留 1 年 |

### 4.2 归档方式

- 将历史数据迁移到归档表或归档库
- 热表只保留近期数据
- 提供归档数据查询接口
